# 虹软人脸识别

## 1.动态载入库 
**方法一  .dll(.so)三个文件 放入java.library.path(jdk安装目录 /bin)**

**方法二  放入指定文件夹 加载引擎指定(new FaceEngine("pwd"))**


## 2.将 jar文件载入开发工程
   module 对应项目 添加 jar

## 3.激活

**`faceEngine.activeOnline(appId, sdkKey)`**


